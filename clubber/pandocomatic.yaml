templates:
  default:
    glob: ['*.md','*.markdown']
    pandoc:
      from: markdown+lists_without_preceding_blankline
      to: html
      standalone: true
      include-before-body: clubber/include-before-body.html